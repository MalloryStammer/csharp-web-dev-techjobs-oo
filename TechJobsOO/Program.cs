using System;

namespace TechJobsOO
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             * Has anyone else used Visual Studio's "extract base class" option? 
             * It seems useful...

            Todd Romkema  11 hours ago
            Like, "it does most of the work for you on a large portion of the 'Create a Base Class' and
            'Extend JobField into Employer' parts of Assignment 2" level of useful
             * 
             * 
             * 
            Job job1 = new Job("Product tester", new Employer("ACME"), new Location("Desert"), new PositionType("Quality control"), new CoreCompetency("Persistence"));
            Job job2 = new Job("Web Developer", new Employer("LaunchCode"), new Location("St. Louis"), new PositionType("Front-end developer"), new CoreCompetency("JavaScript"));
            Job job3 = new Job("Ice cream tester", new Employer(""), new Location("Home"), new PositionType("UX"), new CoreCompetency("Tasting ability"));

            List<Job> jobs = new List<Job>();

            jobs.Add(job1);
            jobs.Add(job2);
            jobs.Add(job3);

            foreach (Job job in jobs)
            {
                Console.WriteLine(job);
            }
            */
        }
    }
}
